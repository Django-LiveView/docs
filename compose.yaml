
services:

  one-el:
    build: .
    restart: "no"
    volumes:
      - .:/usr/src/app/
      
  nginx:
    restart: on-failure
    image: nginx:alpine
    ports:
      - 9100:80
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./public:/var/www/html
